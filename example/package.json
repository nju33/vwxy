{
  "private": true,
  "name": "example",
  "version": "0.1.1",
  "description": "a vwxy example",
  "main": "index.html",
  "scripts": {
    "test": ":",
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "predeploy": "rm -rf .cache dist .DS_Store",
    "deploy": "codesandbox ./"
  },
  "dependencies": {
    "vwxy": "^0.1.1"
  },
  "devDependencies": {
    "@babel/core": "7.2.2",
    "codesandbox": "1.3.8",
    "parcel-bundler": "1.11.0",
    "typescript": "3.2.2"
  }
}
